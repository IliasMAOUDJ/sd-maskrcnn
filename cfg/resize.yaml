dataset:
  path: latim_dataset  # base path of the directory of the dataset
  img_dir: depth_ims # name of the image directory within the dataset folder
  img_out_dir: depth_ims_normalized
  mask_dir: semantic_masks # name of the mask directory within the dataset folder
  mask_out_dir: semantic_masks_scaled # name of the output directory for masks (within the dataset folder)

images:
  resize: 1
  max_dim: 512
  normalize: 1
  out_ext: png

masks:
  resize: 1
  max_dim: 512
